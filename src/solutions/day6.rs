use std::time::{Duration, Instant};

pub fn solve(_input: &str) -> (String, String, Duration) {
    let t = Instant::now();
    /* */
    let p1 = String::from("");
    let p2 = String::from("");
    (p1, p2, t.elapsed())
}
